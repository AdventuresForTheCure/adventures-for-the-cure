.row
  .col-lg-12(ng-controller='volunteerEventCreateCtrl')
    .well
      p This page is for creating volunteer events and is accessible by 'board' members only.
    .well
      h4 Create Volunteer Event
      form.form-horizontal(name="createVolunteerEventForm" role='form')
        .form-group
          label.col-md-2.control-label(for="name") Name
          .col-md-10
            input.form-control(name="name", type="text", placeholder="Name", ng-model="name", required, autocomplete="off" )
        .form-group
          label.col-md-2.control-label(for="date") Date
          .col-md-10
            input.form-control.date(name="date", type="date", ng-model="date", required, autocomplete="off" )
        .form-group
          label.col-md-2.control-label(for="isAfcEvent") AFC Event
          .col-md-10
            input(name="isAfcEvent", type="checkbox", ng-model="isAfcEvent")
        .form-group
          label.col-md-2.control-label(for="image") Image / Logo
          .col-md-10
            input(name="img" type="file" ng-file-select="onFileSelect($files)" accept="image/*", required)
        .form-group
          .col-md-10.col-md-offset-2
            .pull-right
              button.btn.btn-primary(ng-click="createVolunteerEvent()", ng-disabled="createVolunteerEventForm.$invalid || !img") Create
              | &nbsp;
              a.btn.btn-default(href="/") Cancel